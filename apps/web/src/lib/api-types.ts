/** Re-export API types from shared package. Frontend UI types (ProjectsData, etc.) live in src/types/. */
export type {
  ApiListResponse,
  ApiItemResponse,
  ApiProject,
  ApiSkill,
  ApiExperience,
} from "@portfolio/shared";
